projection;
strict;
use draft;

define behavior for ZRK_C_SUP_CON_DATA alias SupConData
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}